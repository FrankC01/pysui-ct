# Session Handoff — pysui-ct

**Last completed:** Task 4 (2026-07-14) — devnet republish + `create-ct-token`/`export-ids` console commands. See `.claude/session-archive.md` for full history.

**Current devnet state:** `contra` package_id `0x2e2ff112f8752a7c7b05498039a431c8dbce1c57b7f6bdce270e759cb312aea1`; `ConfidentialToken<PYSUI_TOKEN>` created and shared at `0x4fb12251b5003bb08efb6f78624bff5890a9692f21e868c94d10e40a7aa736a7`. Devnet is wiped weekly — verify these ids are still live before assuming so (see project memory `project_devnet_weekly_wipe`).

---

*(No active work unit — add a new section when work begins.)*